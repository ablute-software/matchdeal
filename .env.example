# Copy to .env for local dev. Both values are safe to expose to the client:
# the anon key is protected by RLS. The SERVICE ROLE key must NEVER be here —
# it belongs only to the pairing Edge Function.
EXPO_PUBLIC_SUPABASE_URL=https://wkjcaoqdvhykrfacsylr.supabase.co
EXPO_PUBLIC_SUPABASE_ANON_KEY=
